<?php if (isset($tags) && count($tags) > 0): ?>
  <?php foreach ($tags as $current): ?>
  <table class="list_item">
    <tr>
      <td>
        <?=$this->escapeHtml($current['Tag'])?>
      </td>
      <td>
        <?=
          $this->iconButton(
              'trash',
              "Item.unlink('Tag', " . $current['Tag_ID'] . ")",
              'Unlink tag: ' . $current['Tag']
          )
        ?>
      </td>
    </tr>
  </table>
  <?php endforeach; ?>
<?php else: ?>
  No tags set.
<?php endif; ?>
