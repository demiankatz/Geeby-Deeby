<? if (isset($countries) && count($countries) > 0): ?>
  <? foreach ($countries as $current): ?>
    <a href="<?=$this->url('edit/country', array('id' => $current['Country_ID']))?>"><?=$this->escapeHtml($current['Country_Name'])?></a><br />
  <? endforeach; ?>
<? else: ?>
  No countries defined.
<? endif; ?>