<?php if (isset($pseudonyms) && count($pseudonyms) > 0): ?>
  <?php foreach ($pseudonyms as $current): ?>
    <?php $currentName = $this->showPerson($current); ?>
    <table class="list_item">
      <tr>
        <td>
          <a href="<?=$this->url('edit/person', ['id' => $current['Person_ID']])?>">
            <?=$this->escapeHtml($currentName)?>
          </a>
        </td>
        <td>
          <?=$this->iconButton(
            'trash',
            "Person.unlink('Alias', " . $current['Person_ID'] . ", 'pseudonym')",
            'Unlink pseudonym: ' . $currentName
          )?>
        </td>
      </tr>
    </table>
  <?php endforeach; ?>
<?php else: ?>
  No relevant names.
<?php endif; ?>