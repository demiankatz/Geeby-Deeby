<? if (isset($cities) && count($cities) > 0): ?>
  <? foreach ($cities as $current): ?>
    <a href="<?=$this->url('edit/city', array('id' => $current['City_ID']))?>"><?=$this->escapeHtml($current['City_Name'])?></a><br />
  <? endforeach; ?>
<? else: ?>
  No cities defined.
<? endif; ?>