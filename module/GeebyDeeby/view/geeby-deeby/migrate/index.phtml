<? $this->layout()->title = 'Data Migration' ?>
<? if (!empty($messages)): ?>
  <h2>Status</h2>
  <p>
    <? foreach ($messages as $m): ?>
      <?=$this->escapeHtml($m)?><br />
    <? endforeach; ?>
  </p>
<? else: ?>
  <p>No data migrations necessary.</p>
<? endif; ?>