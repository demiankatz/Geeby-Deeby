<?
    $title = $this->fixtitle($series['Series_Name']);
    $this->layout()->title = 'Series Images - ' . $title;
?>
<? if (count($images) < 1): ?>
  <p>No images available for this series.</p>
<? else: ?>
  <?=$this->render('partials/images.phtml', array('images' => $images, 'linkType' => 'item'))?>
<? endif; ?>